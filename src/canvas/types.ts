/**
 * Canvas size configuration
 */
export interface CanvasSize {
  width: number;
  height: number;
}

/**
 * Viewport configuration (for future Phase 2 infinite canvas)
 */
export interface Viewport {
  x: number;
  y: number;
  zoom: number;
}

